import React from "react"

function LeadSalesRep({name}) {

    
    // const nameString = `\n${name}\n`
    // console.log(nameString)

    return (
            <div>
                <td>{name}</td>
            </div>
    )
}

export default LeadSalesRep;